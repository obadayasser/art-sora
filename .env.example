# ArtSora storefront — environment variables
# Copy to .env.local and adjust for your environment.

# Base URL of the ArtSora backend API (see: github.com/obadayasser/artsora-back)
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
